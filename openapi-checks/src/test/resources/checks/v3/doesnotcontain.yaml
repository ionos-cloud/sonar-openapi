openapi: "3.0.0"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /clusters:
    post:
      operationId: clustersCreatePost # Noncompliant: Found operationId: `clustersCreatePost` should not contain both `create` and `post`
      description: Retrieves a list of PostgreSQL clusters.
      summary: Retrieves a list of PostgreSQL clusters 2
      security:
        - basicAuth: []
        - tokenAuth: []
      tags:
        - Clusters
      parameters:
        - $ref: "#/components/parameters/ClusterNameQuery"
      responses:
        "200":
          description: Successful operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterList"
        default:
          $ref: "#/components/responses/ApiError"
